import React, {
	FunctionComponent
} from 'react'

export type FlexingContainerProps = {}

export const FlexingContainer: FunctionComponent<FlexingContainerProps> = ({
}) => {
	return <div>Flexing container</div>
}
